package datatypes

enum class Constants(val value: Float) {
    // Define mathematical constants with their respective values
    PI(Math.PI.toFloat()),
    E(Math.E.toFloat()),
    GRAVITY(9.81f);

    // You can add more constants here as needed

}
